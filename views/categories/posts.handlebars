<h1>{{category.name}}</h1>
{{#each posts}}
<div class="card mb-4">
    <div class="card-body">
        
        <h3>{{title}}</h3>
        <p>{{desctiption}}</p>
        <a href="/post/{{_id}}"><button class="btn btn-success">Read more</button></a>
        <hr>
        <small>Category: {{category.name}}</small><br>
        <small>Posted on: {{date}}</small><br><br>
    </div>
</div>

{{else}}
    <h5>No posts</h5>
{{/each}}